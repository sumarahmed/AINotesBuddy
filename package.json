{
  "name": "notesbuddy",
  "private": true,
  "version": "2026.8.1",
  "description": "A local-first meeting capture app with local or hosted speaker transcription.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server.mjs",
    "build": "node build.mjs",
    "preview": "node server.mjs dist/client",
    "test:unit": "node tests/meeting-audio.test.mjs",
    "test:service": "python -m unittest discover -s services/transcription/tests -v",
    "test:browser": "node tests/browser-smoke.cjs",
    "check": "node --check src/runtime-config.js && node --check src/meeting-audio.js && node --check src/app.js && node --check build.mjs && node --check server.mjs && node --check site-worker.mjs && npm run test:unit && npm run build && node --check dist/client/src/runtime-config.js && node --check dist/client/src/meeting-audio.js && node --check dist/client/src/app.js && git diff --exit-code -- dist",
    "test": "npm run check"
  }
}
